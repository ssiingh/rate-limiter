FROM node:20-alpine

WORKDIR /app

RUN addgroup -g 1001 -S nodejs && adduser -S appuser -u 1001

COPY package*.json ./
RUN npm install

COPY tsconfig.json ./
COPY src ./src

USER appuser

EXPOSE 3000

CMD ["npm", "run", "dev"]
